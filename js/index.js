$('.movie').slick({
    slidesToShow: 4,
    // dots: true,
    // centerMode: true,
    // centerPadding: '40px'
})

$('.video').slick({
    slidesToShow: 2,
})

$('.actor').slick({
    slidesToShow: 4
})